[project]
name = "tedisc-dagster"
version = "0.1.0"
description = "Dagster deployment for the TEDISC project"
readme = "README.md"
requires-python = ">=3.12"

[tool.dagster]
module_name = "tedisc_dagster"
